# 文本文件 (不通过LFS，直接版本控制)
Config/*.ini text
*.h text
*.cpp text
*.cs text
*.md text
*.json text
*.uproject text

# 大文件 (Git LFS管理)
*.uasset filter=lfs diff=lfs merge=lfs -text
*.umap filter=lfs diff=lfs merge=lfs -text
